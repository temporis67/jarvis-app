// this is a empty component for now
// Template: src/app/components/EmptyComponent.tsx

export default function ModelSettings() {

    const model_name = "model_name";

    return (
        <div className={"mt-2"}>
            <h2>Model Settings for {model_name}</h2>
            <p>Settings go here ...</p>
        </div>
    )
}

